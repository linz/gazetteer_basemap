Files used in the LINZ gazetteer application basemap

tilemill: Building the cached tiles forming the raster basemap

labeldata: Build the data supporting the WFS service used to apply labels to the map

openlayers: Example openlayers application using the tile and label services
